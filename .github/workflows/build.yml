on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to a commit predating ZMK's Zephyr 4.1 "board variant" migration
    # (https://zmk.dev/blog/2025/12/09/zephyr-4-1#zmk-board-variant), since our
    # west.yml still pins an older ZMK revision whose pillbug board doesn't use it.
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@c25e927a2fae915e208a8777ce211a10c658e771
